### Portfolio - 장범진
#### 최종 수정일 : 26.09.07

> 개인 작업용 계획서. 
- 구현 목표를 구체화, 일관되도록 하기 위해 작성. README에는 최종 구현한 내용만 정리할 예정.

간단한 프로젝트 목적 : Steam 게임 유통 플랫폼의 특정 게임들의 가격과 리뷰를 수집해 할인 대비 리뷰수의 증감, 누적 평가 비율 변화 조회


## 기본 데이터 처리 구조

Steam 상점 데이터를 원천으로 사용하며, **raw에 원본을 먼저 적재하고 SQL로 변환하는 ELT 방식**을 사용한다.

**왜?**
- 원본을 다시 확인하고 재처리할 수 있도록 먼저 DB에 보관하고, 꺼내쓸 수 있는 효율적인 방법으로 채택.
- Python은 수집·raw 적재·실행 관련에 사용하며, SQL은 정제·집계에 사용한다.

[데이터 처리 구조]
1. 데이터 수집 → PostgreSQL DB에 `raw` 데이터 원본 보관
2. 데이터 전처리 → SQL로 `stg` 정제 결과 생성
3. 데이터마트 생성 → SQL로 집계해 같은 DB의 `mart`에 저장

---

## [들어갈 기능들]


### 0. 실행 환경 준비
1. Docker·PostgreSQL 환경을 사용하고, 처리 시작 전에 실행·접속 상태를 확인. 
   (수집·정제 단계마다 Docker를 다시 실행하지 않기 위함)

2. 지정한 게임의 **가격(price)** 대상으로 수집. `review` 또한 일자별 누적 개수와 긍정/부정 리뷰 수 비율을 파악한다.

3. API 데이터의 컬럼 후보는 `game_id`, `game_name`, `game_price`, `currency`, `collected_at`, `snapshot_date`,`total_review`.
   - `game_id`: 게임 식별자. DB 행에 붙이는 자동 번호와 구분.
   - `collected_at`: 실제 수집 시각. 
   - `snapshot_date`: 데이터의 기준 날짜.
   - 가격(game_price) 단위는 실제 응답 확인 후 확정 필요.
   - `totaL_review`: 누적 리뷰 수. 
4. DB 접속정보와 API 키는 환경변수 (.env)파일로 관리. 예시 설정만 GitHub의 `.env.example`으로 작성.


### 1. 데이터 수집
1. Steam 가격 조회 경로와 응답 샘플을 확인한 뒤, 지정한 게임의 `game_id`를 기준으로 호출. game_id도 같은지 비교한다.
2. **수집 조건 초안:** 한국 상점·KRW 기준 현재 판매가를 수집.
3. 응답 상태 확인 후, 수집 시각과 기준 날짜 입력.
4. Python에서 실행 중인 PostgreSQL에 연결해 수집 결과를 `raw`에 저장한다.
5. 수집부터 mart 검증까지 정상 동작 확인한 뒤, 매일 한국 시간 00시 실행(crontab으로)을 추가. 자동 실행 시 PC·DB 가동 상태와 중복 실행 방지 체크 기능.


### 2. 데이터 전처리
1. `raw` 데이터를 SQL로 조회해 불러온다.
2. `smapshot_data`별, `game_id` 별 정렬 후, `game_price`를 불러온다. 중복되는 경우, `collected_at` 이 더 최신인 pirce를 사용한다.
3. 날짜> Datatime으로, 가격 > 조회되는 화폐 단위에 맞게 타입 변환. 변환 불가능한 값은 오류로 확인만 하기.
4. 결측치는 컬럼별로 처리. 식별자·기준 날짜 등 필수값 누락을 검사. 가격 누락을 임의로 0원이나 평균 가격으로 채우지 않는다.
5. 이상치는 통계적으로 크다는 이유만으로 제거하지 않는다. 숫자 변환 실패, 음수 가격, 다른 통화 혼입 등 정해진 데이터 규칙 위반을 검사.
6. 중복은 전체 컬럼의 동일 여부가 아니라 **같은 관측 대상인지**로 판단.
   - `stg`의 한 행 예시: 고정한 국가·통화에서 특정 날짜의 특정 게임 가격.
   - 중복 기준 예시: `snapshot_date + game_id`. 기존 가상 CSV 테스트에서는 날짜·게임명을 임시 기준으로 사용.
7. 정제 결과는 PostgreSQL `stg`에 저장. 


### 3. 데이터마트 생성
1. 전처리된 `stg` 데이터를 SQL로 조회. 목적에 맞게 집계해 `mart`에 저장한다. 
2. 조회 목적 후보: 지정한 기간의 게임별 평균·최저·최고 가격과 리뷰 수 변화.
3. 첫 mart는 우선 `mart.game_price_summary` 하나만 제작.
4. 직접 계산한 결과와 집계 결과 서로 비교. 집계로 행 수가 줄어들 수 있기에 raw·stg·mart의 행 수를 무조건 같게 처리 x.

### 4. 공통 운영·검증 규칙
1. **멱등성:** 같은 저장 입력을 두 번 처리해도 `stg`의 업무 데이터가 중복되지 않고, `mart` 집계가 누적되지 않도록 확인.
   - 같은 날짜의 새 데이터 예외사항: 기존보다 늦게 수집한 유효 가격으로 갱신. 

2. **품질·정합성:** 같은 실행·처리 범위에서 원천 건수와 raw 적재 건수를 비교한다. 
   정제에서 제외된 행이 있다면 이유와 건수를 남긴다. 필수값 누락·날짜/가격 변환 실패·중복·음수 가격을 검사한다.

3. **성공한 부분까지 저장:** 처리 단계별로 저장,취소할 범위를 정한다. (작업하면서 추후 확정)

4. **오류·0건 처리:** 연결 요청에는 timeout을 두고, 재시도 대상과 횟수를 제한해두기. 데이터 오류는 무한 재시도하지 않고 횟수 정해두기.

5. **기록·재처리:** 실행 식별자, 처리 대상, 시작·종료, 성공/실패 단계, 건수, 오류를 최소 파일 로그로 기록. 
   `etl_job_log` 테이블은 필요한 경우 확장하며, 확장 시 보관한 파일·기준 날짜를 지정해 재처리할 수 있도록 한다.

6. **안전·재현성:** SQL 값은 문자열로 직접 합치지 않고 매개변수로 전달한다.

---

## [개인 작업용 함수·SQL 후보]

> 구현 시 찾아 쓸 함수 목록

| 작업 | 필요한 기능 | 후보 |
|---|---|---|
| API 수집 | 요청, 상태 확인, JSON 해석 | `requests.get()`, `raise_for_status()`, `.json()` |
| DB 적재 | 연결, SQL 실행, 확정·취소 | `psycopg2.connect()`, `cursor.execute()`, `commit()`, `rollback()` |
| 정제 | 필드 추출, 조건 처리, 타입 변환 | 딕셔너리 키 접근 또는 JSON 연산자, `CASE`, `NULLIF`, `CAST` |
| mart | 그룹별 집계 | `GROUP BY`, `AVG`, `MIN`, `MAX`, `COUNT` |
| 검증 | 건수·누락·중복·범위 검사 | `SELECT`, `COUNT`, `WHERE`, `HAVING` |

## [작업·문서 메모]

- **ERD:** 지금은 테이블별 한 행의 의미와 키를 1~2줄로 정하고, 실제 응답과 테이블 관계가 정해진 뒤 필요한 그림만 하기. (구체화x)
- **계획의 깊이:** 전체 기능은 한 줄씩, 현재 기능만 입력·출력·처리 규칙·완료 조건을 5~10줄로 구체화. 함수 분리와 변수명까지 글로 미리 확정하지 않는다.
- **공개 README:** 목적, 구현 구조, 실행 방법, 재실행 정책, 실제 테스트 결과를 정리한다. 함수 후보·미해결 질문·입문 학습 메모는 이 개인 계획서에 둔다.
- **오류 기록:** 정상 입력·재실행·잘못된 입력·중간 실패의 예상 결과와 실제 결과를 남긴다. (같은 오류 발생시 용이하게 대응 및 포폴용)





